# MrLi · 部署文档（GitHub Pages）

本文说明如何把 `mrli` 作品集部署到 GitHub Pages，以及日常如何更新上线。

---

## 1. 当前线上信息

| 项 | 说明 |
|----|------|
| 代码仓库 | https://github.com/898989L/mrliweb |
| 线上地址 | https://898989l.github.io/mrliweb/ |
| 部署方式 | GitHub Actions → GitHub Pages |
| 工作流文件 | `.github/workflows/deploy-pages.yml` |
| 触发条件 | 推送到 `main` 分支，或手动运行 Workflow |
| 访问验证码 | `visit888`（见 `src/data/access.ts`） |

> 免费 GitHub Pages **要求仓库为 Public**。私有仓在免费计划下无法启用 Pages。

---

## 2. 本地 vs 线上差异（重要）

| | 本地开发 | 线上生产 |
|--|----------|----------|
| 地址 | `http://localhost:5173/` | `https://898989l.github.io/mrliweb/` |
| Vite `base` | `/` | `/mrliweb/` |
| 配置位置 | `vite.config.ts` 按 `command === 'build'` 自动切换 | 同上 |

公共资源（视频、头像、demos、简历 PDF）通过 `src/utils/asset.ts` 拼接 `import.meta.env.BASE_URL`，保证子路径部署也能正确加载。

本地预览**生产构建**时，需要带上子路径：

```bash
npm run build
npm run preview
# 浏览器打开提示的地址，一般为 http://localhost:4173/mrliweb/
```

---

## 3. 首次部署（从零配置）

若仓库已按本文配置过，可跳到第 4 节「日常更新」。

### 3.1 准备仓库

1. 本地项目目录为 `mrli/`（本工程根目录）
2. 关联远程：

```bash
cd mrli
git remote -v
# 应看到：
# origin  https://github.com/898989L/mrliweb.git
```

3. 确保仓库为 **Public**（Settings → General → Danger Zone → Change repository visibility）

### 3.2 启用 GitHub Pages

1. 打开：https://github.com/898989L/mrliweb/settings/pages  
2. **Build and deployment → Source** 选择：**GitHub Actions**  
3. 首次推送带 workflow 的代码后，Actions 成功即会自动发布

### 3.3 必要工程文件（已存在）

确认以下文件在仓库中：

```
mrli/
├── .github/workflows/deploy-pages.yml   # 自动构建与发布
├── public/.nojekyll                     # 避免 Jekyll 处理下划线资源
├── vite.config.ts                       # build 时 base=/mrliweb/
└── src/utils/asset.ts                   # 静态资源路径前缀
```

`vite.config.ts` 核心配置：

```ts
export default defineConfig(({ command }) => ({
  base: command === 'build' ? '/mrliweb/' : '/',
  plugins: [react()],
}))
```

---

## 4. 日常更新上线

在本地改完内容后：

```bash
cd mrli
npm run build          # 可选：先本地确认能通过构建
git add .
git commit -m "说明这次改了什么"
git push origin main
```

然后：

1. 打开 Actions：https://github.com/898989L/mrliweb/actions  
2. 查看 **Deploy GitHub Pages** 是否成功（约 1～3 分钟，含大视频时更久）  
3. 打开线上地址，**强制刷新**：`Ctrl + F5`（或无痕窗口）

手动重新部署（不改代码也可以）：

1. Actions → Deploy GitHub Pages  
2. 右侧 **Run workflow** → 选 `main` → Run

---

## 5. 工作流在做什么

推送到 `main` 后，GitHub Actions 会：

1. `checkout` 代码  
2. 安装 Node 20，`npm ci`  
3. `npm run build` 生成 `dist/`  
4. 把 `dist/` 上传为 Pages 产物并发布  

对应文件：`.github/workflows/deploy-pages.yml`

---

## 6. 访问验证码

站点有访问闸口，验证通过后写入浏览器 `localStorage`。

| 项 | 值 |
|----|-----|
| 验证码 | `visit888` |
| 修改位置 | `src/data/access.ts` → `ACCESS_CODE` |
| 本地重置 | 控制台执行：`localStorage.removeItem('portfolio_access_ok_v1')` |

改验证码后需重新 `git push` 才会在线上生效。

> 注意：验证码只挡页面内容。直接访问静态路径（如 `/mrliweb/resume.pdf`、`/mrliweb/demos/...`、视频）仍可能打开。若需更强保护，需改用带鉴权的托管或登录后端。

---

## 7. 改域名 / 改仓库名时要注意

若仓库从 `mrliweb` 改名，或换成自定义域名：

1. 修改 `vite.config.ts` 中生产 `base`  
   - 项目站：`/新仓库名/`  
   - 自定义根域名：`/`  
2. 全站静态路径依赖 `asset()` 与 `BASE_URL`，一般不必逐个改资源路径  
3. 重新 push `main` 触发部署  
4. Pages 设置里确认自定义域名与 HTTPS

---

## 8. 常见问题

### 线上样式和本地差很多

1. 先强制刷新或无痕打开，排除缓存  
2. 看 Actions 是否构建成功  
3. 本地执行 `npm run build`，确认无 CSS 语法报错（生产会压缩 CSS，未闭合括号会导致样式大面积丢失）

### 资源 404（视频 / 头像 / 演示页）

1. 确认访问的是带前缀的地址：`/mrliweb/...`  
2. 确认 `base` 在 build 时为 `/mrliweb/`  
3. 新加的 `public/` 资源是否已提交并推送

### Actions 失败：Pages 不可用

报错类似 *plan does not support GitHub Pages*：

- 仓库需要是 **Public**  
- 或升级支持私有 Pages 的 GitHub 计划  
- 备选：改用 Vercel（仓库里已有 `vercel.json` 可作参考）

### 推送很慢 / 仓库很大

`public/videos/` 体积很大（约数十 MB～近百 MB 级）。会拖慢 clone 与 Pages 上传。长期建议压缩视频或改用外部对象存储。

### 本地 `localhost:5173` 打不开样式

确认开发命令在 `mrli` 目录执行：

```bash
npm run dev
```

不要用 `npm run preview` 的结果和开发地址直接对比路径（preview 走生产 `base`）。

---

## 9. 快速检查清单

上线前：

- [ ] `npm run build` 本地通过  
- [ ] 验证码、简历 PDF、联系方式内容已确认  
- [ ] 已 push 到 `main`  
- [ ] Actions 显示 Deploy 成功  
- [ ] 打开 https://898989l.github.io/mrliweb/ 并强制刷新  
- [ ] 输入验证码后，背景视频、导航、项目演示可正常使用  

---

## 10. 相关文档

- 日常改内容、换素材：仓库上级目录 `操作文档.md`，以及本目录 `README.md`  
- 个人信息数据：`src/data/profile.ts`  
- 项目与演示：`src/data/projects.ts`、`public/demos/`  
